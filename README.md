# typst-ling

Personal Neovim tooling for linguistics-focused Typst workflows.

Current v1 features:

- package-aware function picker for `phonokit` and `synkit`
- `#tree()` reindent command for `synkit`

